'use client'
import React from 'react';

const AddToCart = () => {
  return (
      <div>
        <button className="btn btn-primary uppercase" onClick={() => console.log('hi')}>Add to Cart</button>
      </div>
  );
};

export default AddToCart;